'use strict'

// Dit moet een hele engine worden, dus joehooe

class Player {
	constructor(x, y) {
		this.x = x;
		this.y = y;
	}

	move(dx, dy) {
		// TODO
	}

	isFalling() {
		// TODO
		return false;
	}
}

$(function() {
	var player = new Player(0, 0);
});

